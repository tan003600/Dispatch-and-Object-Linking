class data:
    def __init__(self,x):
        self.x=x
    def __eq__(self,y):
        if self.x==y.x:
            return True
        else:
            return False
a=data(8)
b=data(7)

#both command will work, but only one of them will be noticed in output
a.__eq__(b)
print(a==b)
